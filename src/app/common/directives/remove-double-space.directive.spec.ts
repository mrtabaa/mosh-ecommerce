import { RemoveDoubleSpaceDirective } from './remove-double-space.directive';

describe('RemoveDoubleSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveDoubleSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
