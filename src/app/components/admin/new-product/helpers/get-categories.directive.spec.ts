import { GetCategoriesDirective } from './get-categories.directive';

describe('GetCategoriesDirective', () => {
  it('should create an instance', () => {
    const directive = new GetCategoriesDirective();
    expect(directive).toBeTruthy();
  });
});
