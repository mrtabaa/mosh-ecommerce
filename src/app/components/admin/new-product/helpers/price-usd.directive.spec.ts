import { PriceUsdDirective } from './price-usd.directive';

describe('PriceUsdDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceUsdDirective();
    expect(directive).toBeTruthy();
  });
});
